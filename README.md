# In PowerShell
cd d:\projects\timetable\Src
python start.py
as there is some libraries you might have to install so that command will check and install this libraries 

or 
first run start.py this will download all the missing libararies first then you can run main.py 